server {
    listen 80;

    # Serve static files
    location /static/ {
        alias /app/gis-intro-rcaas-rse/static/;
    }

    # Proxy to Flask
    location / {
        proxy_pass http://web:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
